#include "temperature.h"

/**
 * Temperature values are in degrees Celsius.
 * The temperature indices are evaluated by the AV FC, so make sure AV has the same values.
 */
float temperatures[N_TEMPERATURES] = {1, 5, 10, 15, 20, 25, 30, 37};
